##   设置说明

###  浏览设置
#### 1.浏览翻页模式
-  左右翻页：   左右滑动屏幕查看下一张和上一张照片
-  上下翻页：   参考短视频，上滑查看下一张图片，下滑查看上一张图片

#### 2.日出期间强制最大亮度
-  基于用户的大概地理位置(IP定位)，请求和风天气提供的日出日落时间API，在该日落和日出的时间强制屏幕最大亮度。 使用在开启代理后，可能会获取到错误的日出日落信息。 如果无法获取到用户的地理位置，将使用 6 ～ 18 点的默认日出日落时间。

#### 3.自动隐藏
-  点击照片，进入浏览模式后，将自动隐藏顶部状态栏，标题栏和底部操作栏，从而实现全屏浏览。 

#### 4.过滤视频长度
-  该设置在提供了以秒为单位的过滤机制，设置后将隐藏小于对应时长的视频文件。


#### 5.过滤文件大小
-  该设置在提供了以kb为单位的过滤机制，设置后将隐藏小于对应文件体积的文件。

#### 6.照片属性
-  提供了分辨率、文件体积、照片光圈、曝光时间、ISO、拍摄时间、视频长度等属性，选中该属性后，在浏览照片的时候会在底部显示对应的属性，如果该属性不存在，将不会显示。


### 云同步
#### 1.添加账号允许
-  添加WebDAV账号

#### 2.照片显示备份标记
-  使用WebDAV备份后，将会显示该图片是否已经上传云端

### 自定义
#### 1.语言
-  可选英语和简体中文用于慢图浏览的语言显示

#### 2.最近查看的图片
-  开启后，将会记录用户在慢图浏览的图片的记录(外部应用使用慢图浏览打开图片将不会被记录)

#### 3. 自动更新
-  开启后，每次启动慢图浏览将会检测慢图浏览是否有新的版本可供升级


#### 4. 回收站|最近的图片安全校验
-  当用户设置私密相册的密码后，开启该选项后，查看回收站和最近的图片将要求验证PIN码

#### 5. 显示删除提示对话框
-  开启的情况下，将会弹出对话框提示用户，删除图片是永久删除还是移动到回收站。 关闭该选项后，删除图片默认会移动到回收站

#### 6. 跟随隐藏相册
-  相册提供了隐藏相册，在用户选择了隐藏相册后，将不会在相册列表显示，但是其他应用依然可见。 开启该选项后，照片流水线也将会排除隐藏的相册。

### 缓存
##### 1. 扫描图片视频
-  在系统异常的情况下，慢图浏览列表的图片找不到某一张图片，这个时候使用该功能来修复找不到图片的问题，该功能不保证100%能够将图片扫描并且加入到系统媒体库中。

##### 2. 清理缓存
-  为了加快图片的加载体验，将会生成缩略图。点击该按钮将会清理应用的照片缓存。

### 工具箱
#### 1. 将系统文件管理器发送到桌面
-  安卓系统自带了一个文件管理器，但是该文件管理器并没有快捷方式。 点击该按钮后，将会发送安卓系统自带的文件管理器到桌面。 注意：某些华为设备该功能不可用。安卓系统低于8.0, 无法发送快捷方式到桌面。

#### 2. HTTP文件服务
-  某些情况下，我们没有数据线，但是需要将图片或者视频传输到PC端，打开该功能在局域网设备浏览器打开。我们无法确定用户办公环境、家庭环境、旅途环境，为了安全，我们提供了一个自签的证书，并且该服务升级到了https,在浏览器打开可能提示安全问题，请忽略该问题。未来我们会提供更加安全的证书方案。


#### 3. 私密相机
-  主页使用私密相机： 在应用首页打开该选项后，将使用慢图浏览提供的相机。 该相机拍摄的内容将会保存在应用内部的私密相册中。防止其他应用查看到该图片。
-  从最近的任务中隐藏私密相继： 打开该设置后从快捷方式进入私密相机，回到桌面后，相机将会从最近的任务中隐藏。
-  快速关闭私密相机：  从快捷方式打开的私密相机，使用音量+键将会关闭当前相机。默认情况是使用音量-来拍摄照片。
-  将私密相机发送到桌面： 将私密相机的快捷方式发送到桌面。


#### 4. 将手机壁纸应用于应用背景
-  打开该设置后，在应用的主题为夜间模式的情况下，会将手机壁纸应用于当前应用背景。注意：该设置需要文件管理权限。


### 慢图浏览问题反馈
##### 1.社区昵称
-  您在此可以设置您的社区昵称

##### 2.社区头像
-  您在此可以修改您的社区头像

##### 3.进入社区反馈
-  如果您有更好的想法和使用上的问题，请在社区发帖得到我们的帮助。

### 关于

##### 1. 更新历史
-  查看应用的更新历史记录

##### 2. 用于随机ID
-  为了方便我们更好的手机应用崩溃信息，我们使用了随机用户ID的算法来标记用户。

##### 3. 检查新版本
-  点击后将会为应用检测应用是否发布了新的版本，如果找到了新的版本将会提示用户是否需要更新。

##### 4. 开启日志记录
-  当您使用慢图浏览遇到了一些问题，需要打开该开关记录应用的日志。

##### 5. 导出应用日志
-  当您开启日志记录后，点击该按钮可以将应用的日志提取为zip包。 并且通过微信或者QQ发送给我们。

##  功能使用说明

#### 1. WebDAV备份

慢图浏览目前只支持同步路径的方式同步照片，并且在你的WebDAV目录下面创建一个`/慢图浏览/public/`目录，用于存放公开的照片，未来私密相册都会存放在`/慢图浏览/private/`目录下面。

未来也会支持以日期的方式存放照片，例如`/慢图浏览/public/2021/01/01/`，这样可以方便的查看某一天的照片。目前国内的小米，华为，OPPO，VIVO等手机都是使用目录的方式存放照片的，例如`/DCIM/Camera/`，所以慢图浏览也是使用目录的方式存放照片。
但是Google，三星的相册依然使用的是日期目录的方式来备份。所以慢图浏览会支持两种方式，目录和日期的方式来备份照片。

WebDAV备份功能需要你有一个WebDAV服务器，如果你没有，可以使用[Alist](https://alist.nn.ci/)或者[flydav](https://github.com/pluveto/flydav/)搭建一个。

搭建好Alist Webdav后，需要创建WebDAV服务。

##### Alist 配置
1. 打开Alist，点击左边菜单栏存储。
2. 点击上方添加按钮。
3. 驱动选择： 本地存储
4. 挂载路径： `/webdav` ， 这里的 `/webdav` 可以换成你喜欢的路径。
5. 根文件夹路径： 

   linux 下面配置为 `/home/xiaoming/webdav`

   windows 下面配置为 `C:\Users\xiaoming\webdav`

   macos  下面配置为 `/Users/xiaoming/webdav` 
 
   上面的路径建议配置为你的用户目录下面，其中`xiaoming`可以替换成你自己的用户名， 这样会最大程度避免权限问题。目前Alist 不能配置为根目录`/`，建议配置为用户目录下面，这是一个Alist的一个已知的BUG。

6. 其他的配置如果没有其他的需求，可以不需要修改。

完成上述配置后，在慢图浏览中可以配置账号功能。账号如果上面的挂载路径为`/webdav`，那么WebDAV 服务器地址就是: `http://ip:5244/dav/webdav`， Alist访问webdav必须带上`/dav/`+挂载路径，其中`ip`是你的Alist服务器的IP地址。账号/密码就是你的Alist的账号密码。


##### flydav 配置

1. flydav 的path 配置为 `/webdav` ， 这里的 `/webdav` 可以换成你喜欢的路径。
2. 启动flydav后，可以在慢图浏览中配置账号功能。
3. 如果上面的挂载路径为`/webdav`，那么WebDAV 服务器地址就是: `http://ip:7086/webdav`， 其中`ip`是你的flydav服务器的IP地址，`7086`是flydav的默认端口 。账号/密码就是你的flydav的账号密码。


由于慢图浏览暂时不支持自签名证书，所以如果你的WebDAV服务器使用的是自签名证书，可能会出现无法登录账号的情况。这种情况依然可以使用`Zerotier`或者`frp`，内网穿透的方式来访问你的WebDAV服务器，或者配置`ACME`的https证书来访问你的WebDAV服务器。

